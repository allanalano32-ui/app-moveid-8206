// Fonts configuration for the application
// This file ensures all fonts are properly loaded

// Default system fonts are used
// No additional font imports needed as we use system fonts
export const fonts = {
  sans: '"Geist Sans", ui-sans-serif, system-ui, sans-serif',
  mono: 'ui-monospace, SFMono-Regular, monospace'
};